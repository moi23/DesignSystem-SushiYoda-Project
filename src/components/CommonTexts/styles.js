import styled from 'styled-components';

export const CommonTexts = styled.h1`
  font-size: ${(props) => props.fontSize}px;
  color: ${(props) => props.color};
  font-family: 'Roboto', sans-serif;
`;
